function [pc, dm] = createPointCloud(left, right, stereo_parameters)
  dm = disparity(left, right, ...
    'BlockSize', 5, ...
    'DisparityRange', [0, 256], ...
    'Method', 'SemiGlobal',...
    'UniquenessThreshold', 11);
  imshow(dm);
  colormap(gca, jet);
  pc = reconstructScene(dm, stereo_parameters);
end
